
// This simple function will add the light speed to the user traveled distance

function addTraveledDistance(userData, lightSpeed) {
  userData.distanceTraveled += lightSpeed;
}

export default addTraveledDistance;
